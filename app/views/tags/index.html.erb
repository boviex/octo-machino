<h1>Tags</h1>

<ul>
<% @tags.each do |tag| %>
	<li><%= link_to tag.name, tag_path(tag) %>
		<%= link_to "Delete tag", tag_path(tag), method: :delete, data: {confirm: "Are you sure?"} %>
	</li>
<% end %>
</ul>
<br>

<p><%= link_to "Back to Articles", articles_path %></p>